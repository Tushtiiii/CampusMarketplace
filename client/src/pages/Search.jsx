import React from 'react';
import Home from './Home';

// Search page is essentially the same as Home page with search functionality
// We can create a more dedicated search page later if needed
const Search = () => {
  return <Home />;
};

export default Search;